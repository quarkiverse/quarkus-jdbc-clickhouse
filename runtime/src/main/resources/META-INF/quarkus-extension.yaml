artifact: ${project.groupId}:${project.artifactId}:${project.version}
name: JDBC Driver - Clickhouse
metadata:
  keywords:
    - "jdbc-clickhouse"
    - "jdbc"
    - "clickhouse"
  categories:
    - "data"
  status: "experimental"
  description: "Connect to the ClickHouse database via JDBC/DataSource"